# Bundle patch layer. Applied when a profile lists this bundle (dsh.bundle) or
# via --patch. The host row mounts this package's host half; the browser client
# half auto-mounts because package.json declares dsh.client.platform: "web".
- insert:
    - id: dsh-plan-explorer
      name: dsh-plan-explorer
